import React, { Component } from 'react';

export default class Genus extends Component{
    render() {
        return (
            <div>
                <p>I Too Exists.</p>
            </div>
        );
    };
}